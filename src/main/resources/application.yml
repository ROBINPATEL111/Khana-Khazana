spring:
  application:
    name:Restro Application

  datasource:
    url: jdbc:postgresql://localhost:5433/khana-khazana
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update